# 见机 ChanceKit 设计约定

## 1. 主题
面向关注就业群的普通用户。采用安静、紧凑的桌面阅读工作台，连接状态与实际消息是视觉重心。没有营销首页，没有虚构的账号或群聊。

中文品牌为“见机”，英文品牌为“ChanceKit”，副标题为“不让机会淹没在消息里”。导航顶部展示品牌与副标题，窄屏下副标题在导航按钮下方独占一行。

## 2. 配色
白色阅读面 #ffffff；浅灰侧栏 #f2f4f3；石墨导航 #202925；主文字 #202925；次文字 #59655f；叶绿动作 #246c4f；琥珀等待 #946014；错误 #b53e39。CSS 使用对应 OKLCH token，绿色只承担选中与连接语义。

## 3. 排印
字体为 -apple-system、BlinkMacSystemFont、Segoe UI、PingFang SC、Microsoft YaHei、sans-serif，利用平台原生字形和离线可用性。字号 12/13/14/16/20/28 px；字重 400/500/600/700；字距统一 0；消息行高 1.75，数字等宽。

## 4. 组件
CSS Modules 是唯一组件样式策略。Lucide 是唯一图标库。按钮与输入框圆角 6px，菜单 8px，头像 6px。所有按钮最小 40px 点击高度。hover 使用背景变化，pointer active 缩放 .97；focus-visible 为绿色双像素轮廓。禁用状态降低对比且不能交互。

## 5. 布局
8px 间距节奏。桌面 188px 导航、280px 群列表、弹性消息阅读区；连接页为不浮起的双栏，左边环境与配置，右边二维码与账号。常驻顶部账号状态；消息按时间分隔。导航、列表和正文分别滚动。

## 6. 深度
导航用石墨色，群列表为浅灰，阅读区为白色。分隔线仅用于区域边界。常规区域不使用卡片和投影，只有二维码工具面需要框定。

## 7. 约束
不显示虚构用户数据。未连接时显示真实空状态。所有技术参数放进高级连接折叠区域。危险操作独立确认。消息不能作为 HTML 渲染。不可用的未来功能不显示假按钮。

## 8. 响应式
桌面窗口最低 900x640；浏览器预览兼容 320px。1100px 以下缩小导航和群列；760px 以下导航变顶栏，选群后进入消息页并提供返回。表单不横向溢出。尊重 reduced-motion；键盘触发不动画。

## 9. 后续提示
新增列表沿用白色 #ffffff、14px/500、字距0、行高1.6、6px按钮；次文字 #59655f。新增设置页面采用单层分组与分隔线，不嵌套卡片。状态使用 #246c4f 成功、#946014 等待、#b53e39 错误。品牌“见机”在首屏导航保持可见。
